"""gms_eval — offline LLM trading-behaviour rationality eval (the L2 probe suite).

Depends on ``market_sim`` (the engine) and a self-contained forced-JSON Gemini client.
Run: ``python -m gms_eval.run_eval``.
"""
