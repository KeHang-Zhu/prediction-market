# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.hypothesis/

# runs / eval artifacts — kept in the repo as a recorded data snapshot
# (simulation logs, session snapshots, and eval results).

# secrets / local config
.env

# node / frontend
node_modules/
web/node_modules/
market_sim/server/static/

# user-built scenario templates (from the web builder) — keep the dir, ignore its output
scenarios/templates/*.yaml

# OS
.DS_Store
